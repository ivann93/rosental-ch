{% capture captured_page %}
<div class="page-content">

	<h1 class="title h0">{{ page.title }}</h1>

	<div class="rte">
		{{ page.content }}
	</div>

</div>
{% endcapture %}
{% include 'password-protect' %}