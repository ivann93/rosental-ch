{% comment %}
This section template is generated by a customer page generator script by digitalsprung GmbH
PLEASE DON'T MODIFY THIS
This section template was last created on: <%= dateCreated %>
{% endcomment %}

<div class="page-template-starsign"  data-section-id="{{ section.id }}" data-section-type="page-template">
  {% for block in section.blocks %}

    {% case block.type %}
      
      {% when 'headline' %}
        <div {{ block.shopify_attributes }}>
          {% include 'section-headline', snippet_instance: block, snippet_type: 'block' %}
        </div>

    {% endcase %}
  {% endfor %}

</div>


{% schema %}
{
  "name": "Page Template",
  "settings": [
    
  ],
  "blocks": [
    {
      "type": "headline",
      "name": "Headline",
      "settings": [
        {
          "type": "text",
          "id": "headline_text",
          "label": "Headline Text"
        }
      ]
    }
  ]
}
{% endschema %}